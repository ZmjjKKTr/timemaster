# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（SemVer）。

---

## [v0.2.1] — 2026-05-08

### 新增
- **当前活动指示器**：今日地图顶部新增「现在正在」卡片，实时显示当前时间所在的活动/课程名称、时间段、剩余时间，含脉冲动画边框。
- **日期导航**：时间地图支持浏览任意日期，通过 ◀/▶ 按钮、日期选择器或键盘 ←/→ 方向键切换日期。查看非今日日期时显示「今天」快捷返回按钮。
- **现在时间线**：时间轴上的红色当前时间指示线，自动滚动到当前时间位置。
- **当前时间块高亮**：时间轴上当前时间段自动高亮（红色内阴影边框），一目了然。
- **地图空状态引导按钮**：首次使用时地图页显示三步引导按钮（设置学习时间 → 导入课表 → 添加任务），点击直接跳转到对应标签页。

### 修复
- **课表列表渲染崩溃**：`renderCourseList` 中 `await` 误用在非 async 的 `.map()` 回调内，导致课表标签页 JS 报错、整个页面交互失效。
- **课表导入区默认隐藏**：导入课表面板初始 `display:none`，新用户难以发现导入入口，改为默认展开。
- **事件监听器泄漏**：`renderWeekdayPicker` 和 `renderColorOptions` 每次调用时重复 `addEventListener`，弹窗多次打开后单次点击触发多次回调。

### 修改
- 顶部标签「今日地图」更名为「时间地图」，反映日期导航功能。
- 时间轴空状态提示根据查看日期动态显示（「今天没有安排」/「2026-05-03 没有安排」）。
- 自动刷新定时器仅在查看今天时触发，浏览历史/未来日期时不刷新。
- 今日活动卡片在即使当天无课程/任务时仍能显示「自由时间」「未到学习时间」等状态。

### 部署
- 已部署至 GitHub Pages：`https://zmjjkktr.github.io/timemaster/`
- 代码托管：`https://github.com/ZmjjKKTr/timemaster`
- 更新方式：本地修改后 `git push`，GitHub Pages 1-2 分钟内自动生效
- 数据存储：浏览器 localStorage，换设备/浏览器数据不互通

---
## [v0.2.0] — 2026-05-08

### 新增
- **AI 拍照识别课表**：接入阿里云百炼通义千问 qwen-vl-max 视觉模型，支持上传课表截图/照片，自动识别并提取课程信息（名称、星期、节次、教师、教室、教学周）。
- **一键清空课表**：课程列表卡片头部新增「清空全部」按钮，带二次确认。
- **连续节次自动合并**：同一课程名在相邻节次出现时（如 1-2 节和 3-4 节均为「高等数学」），自动合并为一条记录（1-4 节）。
- **导入方式子标签**：课表导入区拆分为「粘贴文本」「上传文件」「拍照识别」三个子面板。

### 修改
- 数据层重构：引入内存缓存（`_userCache`），`loadData` / `saveData` 改为异步架构，为后续切换后端做准备。
- 拍照识别面板 UI：从预留占位升级为功能完整的 AI 识别入口，API Key 预置开箱即用。
- 文件上传支持拖拽：Excel/CSV 文件支持拖拽到上传区。
- 解析引擎增强：`parseCourseFromCell` 新增「仅课程名 + 教室」的简化格式解析策略。

### 待解决问题
- 无多用户体系：同一设备多人使用时课表混在一起，需手动管理。
- 无数据备份：数据仅存浏览器 localStorage，清除浏览器数据会导致丢失。
- 图片识别依赖 AI API：网络不通或 API 欠费时无法使用，需考虑离线备选方案（如 Tesseract.js）。
- AI 识别偶有误差：复杂排版或手写课表识别准确率不稳定，需人工核对修正。

---

## [v0.1.0] — 2026-05-07

### 新增
- **粘贴文本导入课表**：支持粘贴教务系统导出的详细课表文本，正则解析课程编号、名称、教师、教室、节次、教学周等信息。
- **上传文件导入课表**：支持上传 `.xlsx` / `.xls` / `.csv` / `.txt` 表格文件，自动检测横向表（节次 × 星期）和纵向表（行列表）两种格式。
- **今日时间地图**：可视化时间轴，按颜色区分上课/午休/固定活动/任务/空闲时间段，每小时时间刻度标签。
- **课程管理**：按星期筛选课程列表，手动添加/编辑/删除课程。
- **习惯设置**：每日学习时间、午休时间、节次 ↔ 时间映射、每日固定活动（支持多选星期和自定义颜色）。
- **任务管理**：一次性任务（指定日期）和每周循环任务，按类型筛选。
- **统计卡片**：今日上课/自由/休息/任务时长统计。
- **响应式布局**：桌面端顶部标签导航，移动端底部导航栏自动切换。
- **localStorage 持久化**：所有数据存入浏览器本地存储。

### 修改
- （初始版本，无修改项）

### 待解决问题
- 拍照识别课表仅有 UI 占位，未接入实际 OCR/AI 服务。
- 同一课程连续节次显示为多条记录，阅读不便。
- 无课表清空功能，需逐条删除。
- 无多用户/多设备支持。

---

## 下一步计划

### 近期（v0.3.0）
- [ ] **多用户档案系统**：同一设备上创建/切换多个用户档案，数据按档案隔离。
- [ ] **周视图统计**：以周为单位展示课程分布热力图、各天学习时长对比。
- [ ] **课表导出**：导出为图片或 PDF，方便分享给同学。
- [ ] **数据导出/导入**：JSON 格式导出备份，跨设备恢复。

### 中期（v0.4.0）
- [ ] **云端同步**：接入 Supabase 或 Cloudflare D1 实现跨设备数据同步。
- [ ] **用户认证**：邮箱注册/登录，数据跟随账号。
- [ ] **习惯模板**：预设「考研党」「期末冲刺」「日常学期」等模板，一键切换时间设置。
- [ ] **考试倒计时**：添加考试日期，在今日地图上显示倒计时提醒。

### 远期（v1.0.0）
- [ ] **PWA 离线应用**：支持安装到手机桌面，离线使用。
- [ ] **多人课表对比**：同学之间对比课表，找共同空闲时间。
- [ ] **智能排课建议**：根据空闲时间段自动推荐任务安排。
- [ ] **学习数据分析**：每周/每月学习时长趋势图，时间利用率报告。

---

> **日志维护规范**
> - 每次发版时，在文件顶部 `## [vX.Y.Z] — YYYY-MM-DD` 格式新增版本条目。
> - 「新增」= 全新功能；「修改」= 已有功能的增强或行为变更；「修复」= Bug 修正；「待解决问题」= 已知但本版本未处理的缺陷。
> - 「下一步计划」在每次发版后更新，勾选已完成项，补充新想法。
> - 版本号规则：主版本号（架构级变更）→ 次版本号（新功能）→ 修订号（Bug 修复）。
